# Test Fixtures

Captured USB packets, device `.state` files, and other test data.

- USB packet captures from Wireshark/USBPcap go here
- `alsa-scarlett-gui` demo `.state` files for each supported device
- Mock response data for protocol command tests
