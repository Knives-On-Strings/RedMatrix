export default function Routing() {
  return (
    <div className="flex flex-col items-center justify-center h-full text-neutral-500">
      <h2 className="text-lg font-semibold text-neutral-300 mb-2">Routing</h2>
      <p className="text-sm">Patchbay matrix — sources x destinations, one source per output</p>
    </div>
  );
}
