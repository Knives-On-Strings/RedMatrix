# Scripts

Python + PyUSB prototyping scripts for USB device exploration.

Used during Phase 0 to validate USB access, capture device responses,
and test protocol commands before implementing them in Rust.
